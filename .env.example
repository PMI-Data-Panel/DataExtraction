# ===================================
# OpenSearch 설문 데이터 인덱서 환경 변수
# ===================================

# --- OpenSearch 설정 (로컬/원격 서버) ---
OPENSEARCH_HOST=159.223.47.188
OPENSEARCH_PORT=9200
OPENSEARCH_USER=admin
OPENSEARCH_PASSWORD=AVNS_1unywEqMDAepzpSW6vU
OPENSEARCH_USE_SSL=true
OPENSEARCH_VERSION=2.14

# --- AWS OpenSearch Service (선택사항) ---
# AWS_REGION=ap-northeast-2
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# OPENSEARCH_ENDPOINT=https://your-domain.region.es.amazonaws.com
# OPENSEARCH_INDEX=survey_responses

# --- Qdrant 벡터 DB 설정 ---
QDRANT_HOST=104.248.144.17
QDRANT_PORT=6333
# QDRANT_API_KEY=  # 보안 없으면 비워두기
QDRANT_COLLECTION=survey_vectors

# --- 임베딩 모델 (KURE-v1) ---
EMBEDDING_MODEL=nlpai-lab/KURE-v1
EMBEDDING_DIM=1024

# --- kNN 벡터 검색 설정 ---
HNSW_M=16
HNSW_EF_CONSTRUCTION=512
VECTOR_ENGINE=nmslib

# ===================================
# 사용 방법
# ===================================
# 1. 이 파일을 .env로 복사:
#    Windows: copy .env.example .env
#    Linux/Mac: cp .env.example .env
#
# 2. OpenSearch 서버 시작 (로컬 개발용):
#    docker run -d --name opensearch -p 9200:9200 -p 9600:9600 \
#      -e "discovery.type=single-node" \
#      -e "OPENSEARCH_INITIAL_ADMIN_PASSWORD=Admin@1234" \
#      opensearchproject/opensearch:2.11.0
#
# 3. Qdrant 서버 (이미 실행 중: http://104.248.144.17:6333/dashboard)
#
# 4. 서버 시작: python start_server.py
#
# 5. API 문서: http://localhost:8000/docs
